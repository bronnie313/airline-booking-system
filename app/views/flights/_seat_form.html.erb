<%= form_with(model: seat) do |form| %>
    <% if seat.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>
  
        <ul>
          <% seat.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div>
      <%= form.label :flight_id, style: "display: block" %>
      <%= form.number_field :flight_id %>
    </div>
  
    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>
  
    <div>
      <%= form.label :baggage, style: "display: block" %>
      <%= form.text_field :baggage %>
    </div>
  
    <div>
      <%= form.submit %>
    </div>
  <% end %>
  